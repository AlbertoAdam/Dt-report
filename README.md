# Dt-report
This repository contains all the code used for generating the graphs and the models used for Data Analysis Chapter
In order to run code, the CSV file "sleep_data.csv" need to be saved in same directory with the python files.
